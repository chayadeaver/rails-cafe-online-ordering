<h2><%= @category.name %></h2>

<% @category.items.each do |item| %>
    <p><%= link_to item.name, item_path(item) %> - $<%= item.price %></p>
<% end %>

<p><%= link_to "Add Items to Menu", new_menu_item_path(@category) %></p>