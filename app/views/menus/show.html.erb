<header class="break">
<h2><%= @menu.name %></h2>
</header>


<% @menu.items.each do |item| %>
    <p><%= link_to item.name, item_path(item) %></p>
<% end %>

<p><%= link_to "Add Items to Menu", new_menu_item_path(@menu) %></p>